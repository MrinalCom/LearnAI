---
title: K-Nearest Neighbors
description: An introduction to the KNN algorithm for classification and regression.
order: 1
estimatedMinutes: 15
type: playground
---

import { KnnVisualizer } from "@/components/viz/knn-visualizer";
import { PyodidePlayground } from "@/components/playground/pyodide-playground";
import { Quiz } from "@/components/quiz/quiz";
import { LessonProgressToggle } from "@/components/progress/lesson-progress-toggle";
import { ExplainedTwice, ForBeginners, ForEngineers } from "@/components/mdx/explained-twice";

<LessonProgressToggle courseSlug="classical-ml" moduleSlug="instance-based-and-linear-models" lessonSlug="knn" />

## Overview

K-Nearest Neighbors (KNN) is one of the simplest supervised learning algorithms.
To classify a new point, it looks at the `k` closest labeled points in the
training set and takes a majority vote (for regression, it averages their
values instead).

<ExplainedTwice>
<ForBeginners>

Imagine you move to a new neighborhood and want to guess whether it's a
friendly area. You knock on the doors of the 5 closest houses and ask.
If 4 out of 5 say "yes, friendly," you guess your neighborhood is
friendly too — no research, no formula, just: look at who's nearby and go
with the majority.

</ForBeginners>
<ForEngineers>

KNN has no training phase — it's a non-parametric, instance-based
learner that defers all computation to prediction time. Classifying a
query point requires computing its distance to every stored training
point (`O(n)` per query, `n` = training set size), sorting for the `k`
smallest, then taking a majority vote over their labels. This is why KNN
scales poorly to large datasets without an approximate-nearest-neighbor
index — the same tradeoff the RAG course's vector-search lesson covers
in depth.

</ForEngineers>
</ExplainedTwice>

There's no training phase — KNN just memorizes the training data and does all
the work at prediction time. That makes it easy to reason about, but expensive
to query as the dataset grows.

## Try it

Drag the point below around the canvas. It's classified by majority vote among
its `k` nearest neighbors (the training points, in indigo and amber). The
shaded background shows the full decision boundary for the current `k`.

<KnnVisualizer />

Notice what happens as you move the slider:

- **Small `k` (e.g. 1)** — the boundary is jagged and closely tracks individual
  points. Low bias, high variance: sensitive to noise and outliers.
- **Large `k` (e.g. 15)** — the boundary smooths out, ignoring local structure
  in favor of the global class balance. Higher bias, lower variance.

This is the bias-variance tradeoff, and `k` is the knob that controls it.

## Why start here?

KNN requires no training step, is easy to visualize in 2D, and cleanly
introduces core ML concepts that reappear throughout this course:

- **Distance metrics** — KNN typically uses Euclidean distance, but Manhattan,
  cosine, and other metrics matter a lot for high-dimensional or sparse data.
- **The bias-variance tradeoff** — via the choice of `k`, as shown above.
- **Feature scaling** — since KNN is purely distance-based, a feature measured
  in the thousands (e.g. income) will dominate one measured in single digits
  (e.g. age) unless you standardize your features first.

## In real code

The visualization above is a from-scratch implementation. In practice you'd
use a library like scikit-learn. Run this in the browser (no server, no
install — it's a real Python interpreter compiled to WebAssembly):

<PyodidePlayground initialCode={`import numpy as np
from sklearn.neighbors import KNeighborsClassifier

# Same two-blob shape as the visualizer above, just as real feature vectors.
X = np.array([
    [1.0, 1.5], [1.2, 1.8], [0.8, 1.2], [1.5, 1.6],
    [4.0, 4.2], [4.3, 3.9], [3.8, 4.5], [4.1, 4.0],
])
y = np.array([0, 0, 0, 0, 1, 1, 1, 1])

clf = KNeighborsClassifier(n_neighbors=3)
clf.fit(X, y)

query = np.array([[2.5, 2.7]])
print("Predicted class:", clf.predict(query)[0])
print("Class probabilities:", clf.predict_proba(query)[0])
`}
/>

Try changing `n_neighbors`, adding more points, or moving `query` closer to
one cluster — same algorithm as above, real library, running entirely in
your browser tab.

## Choosing k

There's no universally correct `k`. In practice you pick it with
cross-validation (covered later in [Model Evaluation](/docs/courses/classical-ml)):
try a range of values, measure validation accuracy for each, and pick the `k`
that generalizes best — not just the one that fits the training set closest.

A common starting heuristic is `k ≈ √n`, where `n` is the number of training
examples, adjusted up or down based on how noisy the data is.

## Check your understanding

<Quiz courseSlug="classical-ml" moduleSlug="instance-based-and-linear-models" lessonSlug="knn" />
